/**
 * The frameshift filter allows you to do a copy operation between two filter
 * textures with same source-frames but different destination frames.
 *
 * It simply copies the input rectangle from the input texture to the output
 * rectangle of the output texture
 *
 * @class
 */
export class FrameshiftFilter
{

}
